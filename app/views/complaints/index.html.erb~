<% @complaints.each do |complaint| %>
		<div class="panel panel-default">
                    <div class="panel-heading">
                        <h3><%= complaint.title %></h3></h4> <small>by <%= complaint.user.fullname.capitalize %>  | <%= complaint.user.username %> | <span class="glyphicon glyphicon-time"></span> <%= (complaint.created_at+330.minutes).strftime("%d-%m-%y   %l:%M %p") %> </small></h4>
                    </div>
                    <div class="panel-body">
                        <p><%= complaint.content %></p>
                        <a href="#" class="btn btn-primary">View</a>
                    </div>
                </div>



<% end %>

<%= link_to "New Complaint", new_complaint_path , class: "btn btn-primary" %>
